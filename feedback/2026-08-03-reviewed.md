# 2026-08-03 — reviewed batch

One batch, captured 2026-08-02 and folded into `SKILL.md` on 2026-08-03 — the
filename carries the fold date, per this directory's README. It is the first
batch that came from neither a run nor a transcript parse: Claude Code produces
a periodic usage report aggregating months of sessions, and its 2026-08-02
edition carried five findings about `wrap`.

The batch is reproduced below the ledger. The ledger is what was decided; the
notes are what was observed. **Session counts, workplace tooling and the
report's local filename are redacted** from both — they said nothing the
findings needed, and this repo is public. Redactions are marked in place.

## Ledger — note → fold

| Note | Folded as |
| --- | --- |
| Shell probes produce measurements that look valid and are wrong (≥4 sessions, 5 distinct mechanisms) | §1.6 — a command's output is a claim until you know the command ran, with the five mechanisms named; Seiso's re-derivation bullet points at it; §8 gains *the confident zero*; §9 gains the provenance check |
| A Seiri table proposed deleting refs that no longer exist | Seiri — verify each target exists before listing it; prune before surveying `git branch -r` |
| `gh` assumed on a GitLab remote | §6 — read the host off `git remote -v`; `gh`, `glab`, or stop at the push |
| A push resurrected a deleted branch | **Already covered** by §1.9, which says to re-check the remote immediately before pushing. Unknown whether the run predates that directive; no change made |
| GPG signing and PR auto-merge blocked mid-wrap | **Closed by `0.3.0`**, which moved the signing authorisation into the `Publish` question. Most of the report's window predates it |
| Split wrap into read-only audit, then approve-and-execute | **Rejected.** Reasoning recorded in `CLAUDE.md` under *Design decisions worth not re-litigating* |

One bullet of the still-open 2026-07-31 batch is consumed by this fold: the
deletion gate that listed three branches which did not exist. It is the same
finding as the second row above, seen from inside a run rather than from the
aggregate, and it is what made the row a pattern rather than an anecdote. The
rest of the 07-31 batch stays open in `FEEDBACK.md`.

## What the directive renumbering did to older references

Inserting the probe directive at position 6 pushed the four below it down by
one. The `2026-07-26-reviewed.md` ledger names `§1.8` and `§1.9`; those are now
`§1.9` and `§1.10`. The archive is **not** corrected — it records what was
decided when it was decided, and each of its rows names the directive's content
beside its number, so a reader can still find it. Noted here rather than fixed
there, per *supersede, do not rewrite history*.

## What worked

- The aggregate report is a real second intake for external-run friction, and
  it costs nothing to read. It is not a substitute for capture at the time: the
  verbatim is gone, the per-run dates are gone, and what survives is the shape
  plus a count. But a mechanism that fires on its own beats one that requires a
  self-wrap to reach the file.
- Four of the six rows above were already-known-or-closed. That is the intake
  working, not failing — the report's value was concentrated in the one row
  nothing had caught, and it named it with enough mechanism detail to fold
  directly.

## The notes, as captured

## 2026-08-02 — recovered from the usage report, not from a run

Not session notes. The periodic usage report *[filename redacted]* aggregates
*[counts redacted]* months of sessions, and five of its findings are about
`wrap`. It is the second instance of the
external-run intake being recovered after the fact rather than captured at the
time — the first was the `jsonl` parse behind `feedback/2026-07-26-reviewed.md`.
The verbatim and the per-run dates are gone; what survives is the shape and the
count. Notes below are the report's observations, not this session's.

- **Shell probes produce measurements that look valid and are wrong.** The
  report names four recurring mechanisms across at least four sessions: `$?`
  read after a pipeline (`git rebase … | tail -3; echo exit=$?` reported a
  success that belonged to `tail`, during a wrap); `zsh` word-splitting on an
  unquoted expansion, which produced a false branch survey; a missing
  `LC_ALL=C` on ISO-8859-1 data; a non-persisting `cd` between tool calls,
  which gave bogus `markdownlint` results and nearly committed to `main`. Also
  a `grep` that silently skipped a file classified as binary. `SKILL.md` has a
  rule for retracting a false claim and none for not manufacturing one, in two
  phases — Seiso runs the checks, Seiri reads git — whose output *is* shell
  measurement.

- **The stale-remote-ref deletion was seen externally too.** Same failure as
  the 2026-07-31 bullet; the report lists "a Seiri table proposes deleting refs
  that no longer exist" among wrap's repeating shapes, so it is not a one-off
  of that self-wrap.

- **A push resurrected a deleted branch.** Reported as another instance of the
  same family: state read once, acted on later, no re-read in between.
  `SKILL.md` already says to re-check the remote immediately before pushing.
  Unknown from the report whether the run predates that directive.

- **`gh` was assumed on a GitLab remote.** Observed on `/review`, not on wrap.
  Recorded here because wrap's publication path opens a PR the same way and
  has no host detection, and a repo answering to neither host exists too.

- **GPG signing and PR auto-merge blocked mid-wrap, breaking an otherwise
  automated pass.** Most of the window predates `0.3.0` (2026-07-31), which
  moved the signing authorisation into the `Publish` question. Counted as
  corroboration of why that change was made, not as open friction.

- **What the report says is working**, for balance: `/wrap` is named as the
  spine of the user's session-close rhythm, and the payoff it points to is a
  long handover — tracker comments posted, branches cleaned, several docs
  reconciled *[details redacted]*. The question-shaped gates are read as a
  deliberate choice to gate at decisions and run free elsewhere.

- **The report's own recommendation, recorded as-is and not endorsed:** split
  wrap into a strict read-only audit that outputs a findings table and stops,
  then an approve-and-execute phase. Its rationale is that all four frictions
  above are read-phase problems surfacing during the write phase.
